# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added blacklisted jobs when a player is attempting to create a clan because the blacklisted jobs were checked only on inviting new members.
2. Modified data triggers from using `TriggerClientEventAsyncByCoords` to `TriggerClientClanUpdateEvent` on not required data to be sent for everyone but only on the updated clan.
3. Fixed an issue when kicking a member from the clan this member still had access to the clan menu and was considered in the clan until this person had to relog than immediately be removed from everywhere.
4. Added a notification to be sent on the kicked member because this person was never notified if was online that he / she got kicked out from a clan.
5. Fixed an issue when adding a new clan member, the new player was not added for all online clan members until they had to relog.
6. Modified `PLAYER_TARGET_INVITE_COOLDOWN` locale to not display minutes but converted time, such as `"1 day and 30 minutes left"`.
7. Fixed multiple issues and errors on territories related to blips, notifications and general updates that would not work properly.
8. Fixed on territories configuration file some minute comment displays for bandit attacks that were wrong.
9. Added an extra feature on clan management to allow or not allow the clan object reposition. If allowed, a new prompt is added to allow the leader (owner) of a clan to reposition the clan object placement x,y,z,pitch,roll,yaw. Also, a command has been added for administrators but also for clan owners in case they want to reposition a clan far than their current base location - we would support item instead of a command but it is not possible because the metadata would be used only for latest VORP inventory and not old ones. 
11. Added an extra system to check if the clan flag object contains `backticks (`...`)` or a string `"", ''` in order to load the model properly.

## Modified Files:

- client/main.lua
- client/functions.lua
- client/builder.lua
- server/main.lua
- server/raids.lua
- server/territories.lua
- server/commands.lua
- server/functions.lua
- server/nui/members.lua
- locales.lua
- fxmanifest.lua

## Notes

1. Add new locales:

```lua
['PLAYER_SELF_BLACKLISTED_JOB_CREATE'] = { text = "You are not allowed to have a clan while you belong to a blacklisted job.", duration = 5}, -- version 1.0.9
['PLAYER_TARGET_BLACKLISTED_JOB_CREATE'] = { text = "You cannot create a clan for this player while he belongs to a blacklisted job.", duration = 5}, -- version 1.0.9
['KICKED_OUT_FROM_CLAN'] = { text = "You have been kicked out of the clan you belonged to.", duration = 10}, -- version 1.0.9
['NOT_LEADER_TO_REPOSITION'] = { text = "You are not the leader of this clan to reposition the clan base.", duration = 5}, -- version 1.0.9
['CANNOT_REPOSITION_TOO_FAR'] = { text = "You cannot reposition your clan that far while your clan has furnitures in that area.", duration = 5}, -- version 1.0.9

['REPOSITION_1'] = "Press ~e~LEFT - RIGHT~q~ Arrow To Rotate.", -- version 1.0.9
['REPOSITION_2'] = "Press ~e~DOWN - UP~q~ Arrow To Change Yaw Positions.", -- version 1.0.9
['REPOSITION_3'] = "Press ~e~PAGE DOWN - PAGE UP~q~ To Change Pitch Rotations.", -- version 1.0.9
['REPOSITION_4'] = "Press ~e~BACKSPACE - ENTER~q~ To Change Roll Rotations.", -- version 1.0.9
['REPOSITION_5'] = "Press ~e~G~q~ To Place, ~e~R~q~ To Reset or ~e~DELETE~q~ To Cancel.", -- version 1.0.9
```

2. Modify the following locale:

```lua
-- FROM
['PLAYER_TARGET_INVITE_COOLDOWN'] = { text = "You cannot invite this player again for another %s minutes.", duration = 5}, -- version 1.0.8

-- TO
['PLAYER_TARGET_INVITE_COOLDOWN'] = { text = "You cannot invite this player again for another %s.", duration = 5}, -- version 1.0.8
```
(New Updated Locale Notify)
<img width="914" height="60" alt="εικόνα" src="https://github.com/user-attachments/assets/df2d89b4-43bf-45db-b5ca-b8ebc20cc8cd" />

3. Add on the `config/config.lua` file the new option to allow clan reposition:

```lua
-- Set to false if you want to disable the option to let the clan owner to reposition the clan object / clan position.
Config.ClanObjectReposition = true -- version 1.0.9

-- What should be the maximum distance for the reposition when having furnitures on this area?
-- If the clan owner / leader wants to move the clan object farther, all furnitures must be removed completely.
Config.MaximumRepositionDistanceWhenFurniture = 50.0 -- version 1.0.9

```

**AND**

Add the new prompt key action on `Config.PromptKeys` which is `REPOSITION`

```lua
Config.PromptKeys = {
    ['MANAGEMENT']  = { Label = 'Checkout',   Key = 0x760A9C6F, HoldMode = 1000 },
    ['BUILD_MODE']  = { Label = 'Build Mode', Key = 0xC7B5340A, HoldMode = 1000 },
    ['RAID']        = { Label = 'Start Raid', Key = 0xD9D0E1C0, HoldMode = 2000 },
    ['REPOSITION']  = { Label = 'Reposition', Key = 0xD9D0E1C0, HoldMode = 2000 }, -- 1.0.9 (NEW)
}
```

**AND**

Add the new command for repositioning in case the leader or an admin wants to reposition a clan too far from its base

```lua
    ['repositionclan'] = { -- 1.0.9
        
        Suggestion = "Perform this command to reposition a clan.",
        CommandHelpTips = { { name = "Clan Id", help = 'The clan id' } },

        Webhook = { 
            Enabled = false, 
            Color = 10038562,
        },
        
        Groups  = { 'admin', 'mod' },
        DiscordRoles  = { 670899926783361024 },

        -- Set to false if you want only admins to be able to execute this command, otherwise clan leader will also too
        -- this is helpful in case the clan leader wants to reposition the clan far from its base.
        AllowClanLeaderToExecuteCommand = true,
    },
```
<img width="1109" height="755" alt="εικόνα" src="https://github.com/user-attachments/assets/c1c49381-edb6-43f4-bbcc-68f1ec2abf90" />

## How to update?

- You can easily update the modified files by redownloading from the **Cfx.re Portal** and copy-paste on the mentioned directory file paths.
- If new options have been implemented to the configuration file or edited, **DO NOT** copy-paste the new configuration file, otherwise you will get the latest configuration but you will lose all the changes you have made. The proper way updating a configuration file is by finding the new configuration options or the modified ones and do it **manually**.

*Cfx.re Portal **DOES NOT** force-update your configuration file or any files, if it did, you would always lose all the configuration changes you made or would totally break the script, all files are updated or edited manually.*
